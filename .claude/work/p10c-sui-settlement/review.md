## Blocking
- apps/api/src/infrastructure/chain/chain-submitter.ts:91 the `simulatedAbort` pattern is written with `d+` and `w+` rather than `\d+` and `\w+`, so it matches only the literal letters and never a real message (checked: it fails against `MoveAbort in 1st command, abort code: 0, in '0x..::escrow::hold'`). Every Move abort the SDK raises while building, which the brainstorm records as the shape that actually occurs, is rethrown as a fault instead of `INSUFFICIENT_FUNDS` or `SYSTEM_PAUSED`; the spec's "failures are values" invariant does not hold on that path. No test exercises `simulationFailureOf`: the contract suite never reaches a chain abort because the ledger mirror pre checks first.
- apps/api/src/infrastructure/chain/account-address.directory.ts:65 `row.accountId as AccountId` casts to a branded type outside its constructor. docs/09 allows only `as const` and the branded constructors; `accountIdOf` exists and the adapter already uses it (sui-settlement.adapter.ts:118). No other file outside identifiers.ts carries such a cast.
- apps/api/src/infrastructure/chain/ptb/escrow-calls.ts:40,91,113,148,210 and custody-calls.ts:18 take a bare `amount: bigint` or `appraisedValue: bigint` that is in coin base units, ten thousand times the minor units every other bigint in the api carries. docs/09 "names carry units" names a bare `amount` as the case to avoid, and the builders are the one place the two scales meet.

## Non-blocking
- apps/api/src/infrastructure/chain/chain-reads.ts:50 and apps/api/src/infrastructure/chain/publish/publish-package.ts:91 read the wall clock (`Date.now()`, `new Date()`). docs/09 confines that to the clock adapter; the ESLint rule enforces it only under `src/domain`. The polling stopwatch cannot take `ClockPort` (a fixed clock never runs out of patience), so record the exception in docs/09 rather than change the code; `publishedAt` could take the clock.
- apps/api/src/infrastructure/chain/pending-reference-patches.ts:6 the comment cites "the chain lifecycle test" asserting no pending token survives a commit. No such test exists yet: sui-unit-of-work.integration.spec.ts:104 covers outbox and audit rows, and the `loan` column patch has no test. Either that test lands with the next slice or the comment should name the one that exists.
- apps/api/src/infrastructure/chain/chain-execution.ts:135 `eventName` is exported and unused.
- commit 63e1b69 subject is exactly 72 characters. docs/12 prose says under 72; its commitlint rule and the hook allow 72, so the machine passed it.

Verified green: `pnpm check` (no dependency violations, prose clean), the chain and config unit suites (26 tests), and both integration suites against the single validator localnet (16 tests, 54s). Abort codes match all four Move modules constant for constant; every builder's argument list matches its Move signature; the unit of work submits at most once per `run`, inside the database transaction, and never on a rejected Result; pending references are patched in the loan column, the outbox payload, and both audit columns, which is every column a reference lands in; the adapter writes the mirror before appending and returns the chain reference; amounts scale by exactly `10^(decimals - 2)` with a divisibility check on the way back.

## Verdict
BLOCKED
