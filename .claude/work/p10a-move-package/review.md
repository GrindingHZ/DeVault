## Blocking
- none

## Non-blocking
- packages/move/sources/escrow.move:249 `transfer_new` emits `FundsTransferred` before `share_wallet` (line 271) emits `WalletOpened`, while `deposit_new` (line 124) and `pay_new` (line 213) announce the wallet before the money. Three ways of opening a wallet in one module; routing all three through `share_wallet` after the funds land would give one order and one emit site.
- Commit cc4524f `fix(move): open a wallet once when a transfer creates it` also carries the unit comment at packages/move/sources/custody.move:22, which docs/12 would have as its own commit. Recorded only; docs/11 does not rewrite history.
- apps/api/src/infrastructure/chain/move-fixtures.spec.ts:3 still imports `@depawn/test-support/src/fixtures` and `src/move-fixtures` where the other four api files use the barrel. The tsx reason written at apps/api/scripts/generate-move-fixtures.ts:2 does not apply to a vitest spec, so this one can use the barrel.
- packages/move/sources/interest.move:39 `clamp_elapsed` aborts on underflow when `matures_at_ms < started_at_ms`, where interest-calculator.ts returns a negative amount. Move is the safer reading; unchanged since the first review and no fixture covers it.
- packages/move/sources/escrow.move:116 `deposit_new` and :238 `transfer_new` remain outside the spec's escrow table. Both are now tested and both take the operator capability, so nothing weakens; recorded as scope the spec did not ask for.
- Commit c7bff39 carrying sources/escrow.move under a `test(move)` header is settled by docs/11 (no amend, no rebase) and recorded at .claude/work/p10a-move-package/plan.md:29. Not a finding.
- The working tree holds uncommitted api work outside this slice's commits (apps/api/src/infrastructure/chain/ chain client, submitter, PTB builders and unit of work, three migrations under apps/api/prisma/migrations/20260825120000 to 122000, edits to sui-settlement.adapter.ts, persistence.module.ts and main.ts). None of it is in a1e6727 or in this slice's plan, so it is not assessed here; this verdict covers the committed Move package and the api fixture files the earlier passes reviewed.
- Verified on the third pass: a1e6727 touches only tests/escrow_tests.move and tests/config_tests.move (20 insertions), its message is one line under 72 characters with a listed scope; `sui move test` passes 56 of 56; `bash scripts/check-prose.sh packages/move` and `bash scripts/check-boundaries.sh` are clean. Every `assert!` in escrow.move has an expected failure that reaches it: 111 (`a_zero_deposit_is_refused`), 153 (`a_hold_without_a_key_is_refused`), 175 (`a_refund_into_a_stranger_wallet_is_refused`), 193 (`a_zero_payment_from_a_payout_is_refused`), 194 (`a_payment_beyond_the_payout_is_refused`), 221 (`a_release_with_funds_left_is_refused`), 276 (`a_zero_hold_is_refused`), 277 (`a_hold_beyond_the_balance_is_refused`, `a_transfer_beyond_the_balance_is_refused`); and in config.move: 98 (`a_fee_above_the_whole_is_refused`), 99 (`a_liquidation_fee_above_the_whole_is_refused`), 100 (`a_loan_to_value_cap_above_the_whole_is_refused`), 140 (`assert_not_paused_aborts_while_paused`, and through escrow.move:152 `a_hold_while_paused_is_refused`). The two new tests abort at their intended lines and not at an earlier site sharing the code: the payout test holds 5_000 from a 5_000 wallet, so `take` at 277 passes and only 194 can refuse 5_001; the fee test passes 200 as the origination fee, so 98 passes and only 99 sees 10_001. Confirmed mechanically in a scratchpad copy of the package with line 194 recoded to abort 77 and line 99 to abort 88: exactly those two tests failed, each reporting the recoded number, while the earlier `EInsufficientFunds` and `EBadParameters` tests still passed. Earlier verified facts stand unchanged: `transfer_new` emits exactly one `WalletOpened` and tests/escrow_tests.move:273 asserts the count inside its own transaction; every `assert!` site in custody.move (lines 100, 101, 121, 133, 134, 145, 160, 176, 199) has an expected failure; `assert_not_paused` is called from escrow.move:152 and nowhere else in sources; `Payout` has no abilities and only `finish_release` consumes it; `withdraw` transfers only to `wallet.owner`; `config::demo_parameters` matches demo-parameters.ts in itemCategories order; the claim at escrow.move:190 that the close use case drops zero lines is true (close-liquidation.use-case.ts:98, Q-019); Q-006, Q-012, Q-019 and flows 6, 11, 16 cited in comments all exist.

## Verdict
APPROVED
