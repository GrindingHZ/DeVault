## Blocking
- apps/api/test/chain-lifecycle.integration.spec.ts:249 and apps/api/test/chain/chain-inspector.ts:49 the suite is not green. Run one (all three chain suites) failed the first test with `RpcError: missing tx_seq_digest row for tx_seq 191127` thrown out of an inspector read; run two (the suite alone) failed the first and second tests with `newEvents` answering `[]` for a transaction whose object state the test had just read back (wallet funds at line 247, receipt holder at line 379). `getObject` reads the live object store while `listEvents` reads an index the node fills after execution; `newEvents` reads that index once with no wait on the digest the test already holds, and `boundedChainRead` (chain-reads.ts:27) retries only its own timeout, so a node side `RpcError` ends the test on the first attempt. The P10 exit test passes only when the node's index wins a race. Wait on the digest's events (`getTransaction(digest).events`, which `digestResolves` already polls) or poll `newEvents` until the expected digest appears.
- apps/api/src/infrastructure/parameters/sui-protocol-parameters.adapter.ts:18 the comment says a future dated version "reaches the chain when a later write or a restart applies it, which docs/OPEN-QUESTIONS.md records". Neither holds: `appendSetParameters` has one caller, `writeVersion` at line 45, so nothing at boot or publish writes `Config`, and a version dated ahead of its write reaches the chain only if another version is written after it takes effect; and docs/OPEN-QUESTIONS.md has no such entry (Q-022 is a second process learning an edit, the last entry is Q-030). The spec's adapters section asks for the open question to be recorded, and a comment describing behaviour that does not exist breaks docs/09 harder than one restating code.

## Non-blocking
- commit 6cbaee9 is typed `test(api)` and carries a new adapter (sui-domain-event-publisher.ts) and the platform-services wiring. docs/12 defines `test` as tests only and docs/11 line 88 keeps tests out of the implementation commit; the plan says the hook refused a 74 character header, and the remedy for that is a shorter header. docs/11 line 417 forbids rebasing, so no fix commit can repair it; recorded so it stops here.
- apps/api/test/chain-pause.integration.spec.ts:132 the spec's pause test wants holds aborting on chain and refunds, releases and claims passing. The refusal here comes from the row check at the use case entrance, so no hold reaches the paused `Config`, and release and claim while paused are not exercised; the Move suite carries the abort, and flow 11's never blocked list is proven on the database driver only.
- apps/api/test/chain-lifecycle.integration.spec.ts:346 and :407 the escrow events after the borrower's deposit, the repayment and the bid are read and discarded, so the repayment `Paid` is checked by balance only; the pending token sweep at line 453 covers the loan column, the outbox and `chain_settlement` but not the two audit columns `patchPendingReferences` rewrites.
- apps/api/prisma/migrations/20260825123000_chain_receipt/migration.sql:5 no `id` column and no foreign key from `receipt_id` to `custody_receipt`, both asked for in docs/09. Same shape as `chain_account_address` and `chain_funds_hold` from p10b, which the previous review let stand.
- docs/10-flows.md:55, :96 and :245 the Phase 3 paragraphs of flows 1, 2 and 5 still describe a transferred receipt, a `Listing` object and `loan::repay`; the slice that made the spec's shared receipt and escrow transfers real left them as they were (definition of done item 8).

Verified: `pnpm check` green (16 tasks, no dependency violations, prose clean); the 23 chain unit tests green; `sui-custody-port` (11 tests including the object assertion) and `chain-pause` (1 test) green against the localnet; `chain-lifecycle` red in two runs out of two, each time at a different event read. During every run the working tree carried an uncommitted `schema.prisma` change and an untracked `20260825130000_chain_indexer` migration from work outside the reviewed range, and the compose stack was up against the same localnet. The three previous blocking findings are resolved: `simulatedMoveAbortOf` has `\d` and `\w` with four cases in chain-submitter.spec.ts, `accountIdOf` replaces the cast at account-address.directory.ts:66, and every builder takes `baseUnits` or `appraisedValueBaseUnits`. All eight custody methods write the row first and append the Move call with the matching precondition (`transfer_holder`, `encumber` and `burn_for_redemption` on IN_VAULT; `release_encumbrance` and `claim` on ENCUMBERED; `burn_for_liquidation` and `reissue_to_buyer` from either live state), each rejection exercised by the contract suite; pause and unpause append to the same unit of work's transaction; `writeVersion` skips a version whose `effectiveAt` is after the clock; the publisher writes the outbox before one `attest` per event with `pending:` references written as `self`, reachable because `ChainSettlementRef.toJSON` now serialises the reference. Commit subjects are all under 72 characters (longest 70) with scopes from the list.

## Verdict
BLOCKED
